services:
  app:
    image: localhost/goldfish:latest
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      REDIS_ADDR: redis:6379
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
